//importando lo requerido para crear el contexto
import { createContext } from "react";

// crear una instancia del contexto
const context = createContext({});

// exportar el contexto
export default context;
